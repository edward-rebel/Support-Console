# Local dev: the web dev server (5173) calls the standalone API (4000).
# In the combined Railway deployment this is unset, so the app uses same-origin
# relative URLs and the API serves the built SPA.
VITE_API_URL=http://localhost:4000
